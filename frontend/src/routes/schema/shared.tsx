import { PATHS } from "../paths";

import UserProfile from "@/screens/UserProfile";

import AccountBoxIcon from "@mui/icons-material/AccountBox";
import { RouteProps } from "..";

const shared: RouteProps[] = [];

export default shared;
